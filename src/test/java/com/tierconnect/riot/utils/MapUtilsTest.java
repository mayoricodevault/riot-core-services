package com.tierconnect.riot.utils;

import static org.junit.Assert.assertEquals;

import java.util.LinkedList;
import java.util.List;

import org.junit.Test;

import com.tierconnect.riot.map.MapUtils;

public class MapUtilsTest {
	@Test
	public void testHashAndEquals(){
//		List<CassandraThing> things = new LinkedList<>();
//		
//		for(int i=0;i<10;i++){
//			CassandraThing thing = new CassandraThing();
//			thing.setX((float) i);
//			thing.setY(0f);
//			things.add(thing);
//		}
//		assertEquals(1 ,MapUtils.clusterize(things, 100f).size());
//		assertEquals(10,MapUtils.clusterize(things, 1f).size());
//		assertEquals(5 ,MapUtils.clusterize(things, 2f).size());
	}
}
